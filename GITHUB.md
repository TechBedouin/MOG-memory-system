# GitHub publish kit

## Repository name

```
mog-memory
```

**URL:** `https://github.com/TechBedouin/mog-memory`

Short, npm-friendly, searchable. Alternates if taken: `markdown-object-graph`, `mog-agent-memory`.

---

## Description (GitHub repo subtitle, ≤350 chars)

```
Markdown Object Graph — vectorless agent memory for Cursor & Claude. Compile state into git, boot in 400 tokens, lint with `mog`. No Pinecone. No Mem0 API.
```

---

## Topics (GitHub tags — max 20)

Copy-paste into repo **Topics**:

```
ai-agents
agent-memory
cursor
claude
claude-code
llm
markdown
knowledge-graph
context-engineering
developer-tools
coding-agent
open-source
memory
mcp
cline
windsurf
no-vector-db
git
```

**Primary 10 (if you want a tight set):**

```
ai-agents
agent-memory
cursor
claude
markdown
knowledge-graph
context-engineering
coding-agent
no-vector-db
developer-tools
```

---

## npm keywords (package.json — already set)

Same as primary topics.

---

## Social / launch

| Channel | Hook |
|---------|------|
| **README H1** | Git for agent memory |
| **Tweet/X** | Your agent doesn't need a vector DB. It needs a compiler. `npx mog init` |
| **YouTube** | "I deleted my agent's memory API" — 3-session demo + `mog graph` |
| **HN title** | Show HN: MOG – git-diffable memory graph for coding agents (no embeddings) |

---

## Badges to add after publish

- npm version
- GitHub stars
- CI passing

---

## What NOT to put in the public repo

- Personal `soul.md` / client pipeline
- Sales playbooks
- Real phone numbers or prices

This folder (`projects/MOG-memory-system`) **is** the public repo — rename to `mog-memory` when pushing.
